---
title: "Шаблон карточки источника данных"
sidebar_label: "Шаблон карточки"
tags: [data-source, meta]
last_updated: 2026-05-10
draft: true
---

# Шаблон карточки источника данных

Служебная страница для авторов и агентов: при добавлении или правке карточки копируйте **блок ниже** (содержимое fenced-блока `markdown`) в файл карточки, замените плейсхолдеры и удалите неприменимые разделы целиком (без пустых заголовков).

**Путь к файлу:** для `international` и `civil` — `wiki/data-sources/<подраздел>/<slug>.md`. Для **региональных** карточек источника — `wiki/data-sources/regional/<fed_okrug_slug>/<subject_slug>/<slug>.md` (округ и субъект из [`_dev/regional-directory.yaml`](../../../_dev/regional-directory.yaml)); сводная страница субъекта — `subject-<subject_slug>-sources-overview.md` в той же папке `<fed_okrug_slug>/<subject_slug>/`. Для **федеральных** источников — `wiki/data-sources/federal/<группа>/<slug>.md`, где `<группа>` одна из: `catalog`, `budget`, `zakupki`, `programs-orgs`, `statistics`, `nalog`, `legacy` (см. оглавление `wiki/data-sources/federal/README.md`).

**Подраздел:** `federal` — федеральные порталы и выгрузки; `regional` — региональные и муниципальные источники; `international` — международные и межгосударственные официальные витрины с полезными рядами по РФ или сравнениями по странам; `civil` — гражданские агрегаторы и проекты сообщества.

Публичный URL: `/data-sources/<подраздел>/<slug>`, где `<slug>` — имя файла без `.md` (для федеральных карточек в URL **нет** сегмента `<группа>/`). Для новых карточек в frontmatter укажите `slug` тем же путём (см. таблицу ниже), чтобы перекрёстные ссылки из глоссария, отчётности и карточек ИС оставались стабильными.

### Поля frontmatter

| Поле | Обязательно | Пример / значения |
|------|-------------|-------------------|
| `title` | да | `"Открытые данные Минфина России"` |
| `sidebar_label` | да | Короткая подпись для сайдбара |
| `tags` | да | Первая метка: `data-source`; далее `federal` \| `regional` \| `international` \| `civil` и при необходимости тема (`budget`, `procurement`, `subsidies`, `tax`, …) |
| `last_updated` | да | `2026-05-10` |
| `slug` | да для новых карточек | `/data-sources/<подраздел>/<slug>` — тот же `<slug>`, что имя файла (для `federal` путь в файловой системе может быть `federal/<группа>/<slug>.md`, в `slug` — всё равно `/data-sources/federal/<slug>`) |
| `content_type` | да | `data_source` |
| `entity_type` | да | `data-source` |
| `source_url` | по возможности | Канонический URL витрины, каталога наборов или главной страницы источника |
| `data_source_kind` | рекомендуется | `portal` — веб-витрина; `api` — программный интерфейс; `files` — файловые наборы по ссылкам; `ftp` — FTP/SFTP; `mixed` — несколько каналов; `ui_only` — только интерфейс без стабильных выгрузок |
| `jurisdiction` | рекомендуется | `federal` \| `regional` \| `municipal` \| `civil` \| `multilevel` |
| `access` | рекомендуется | `open` \| `registration` \| `restricted` |
| `formats` | по возможности | `["csv", "json", "xml", "xlsx"]` — фактически встречающиеся форматы |
| `status` | по возможности | `current` \| `legacy` \| `deprecated` — для устаревших описаний или зеркал |
| `last_verified` | рекомендуется | `2026-05-14` — дата последней редакционной проверки карточки (см. [измерения качества](/reference/data-quality-dimensions)) |
| `rag_priority` | опционально | `low` — снижает вес строки в лексическом baseline `npm run qa:retrieval` и в индексе для RAG; для массовых черновых карточек из `npm run import:regional-table` выставляется автоматически, после вычитки удалите или замените |
| `data_completeness` | опционально | `full` \| `partial` \| `unknown` — после согласования линтером |
| `machine_readability` | опционально | краткая метка канала доступа (см. справочник измерений) |

````markdown
---
title: "<Полный заголовок карточки>"
sidebar_label: "<Короткая подпись>"
tags: [data-source, federal]
last_updated: YYYY-MM-DD
slug: /data-sources/<подраздел>/<slug>
content_type: data_source
entity_type: data-source
source_url: "https://..."
data_source_kind: mixed
jurisdiction: federal
access: open
# formats: ["csv", "json"]
# status: current
# rag_priority: low
# related_information_system: "/information-systems/federal/example"
---

# <Повтор title из frontmatter>

| Признак | Значение |
| :--- | :--- |
| Уровень и охват | <федеральный / региональный / муниципальный / гражданский / несколько уровней — одной фразой> |
| Тип доступа | <портал, API, файлы, FTP, смешанный — согласованно с `data_source_kind`> |
| Основные форматы | <CSV, JSON, XML, XLSX, HTML, …> |
| Оператор | <орган или организация> |
| Режим доступа | <открытый / по регистрации / ограниченный> |

Коротко: <1–2 предложения: что можно получить из источника и для какой задачи аналитика открытых финансовых данных он первым делом подходит.>

## Качество и верификация

Заполняйте по определениям на странице **[«Измерения качества источника данных»](/reference/data-quality-dimensions)**.

| Измерение | Значение для этой карточки |
| --- | --- |
| Дата проверки (`last_verified`) | <YYYY-MM-DD или «не проверялось» — тогда оставьте черновик `draft: true` до проверки> |
| Полнота (`data_completeness`) | <полная / частичная / неизвестна + пояснение> |
| Машиночитаемость (`machine_readability`) | <API / CSV / HTML / PDF / смешанный + форматы> |
| Юридическая значимость (`legal_significance`) | <официальный оператор / зеркало / гражданский агрегатор + пояснение> |
| Задержка обновления (`update_lag`) | <оценка лага относительно факта> |
| Глубина архива (`archive_depth`) | <горизонт лет / разрывы> |
| Лицензия (`license_or_terms`) | <условия или «не указаны явно»> |

## Описание

<Чуть подробнее: какие разделы портала или какие типы выгрузок относятся к этой карточке; что сознательно **не** входит в описание (например внутренние кабинеты).>

## Оператор

<Орган или оператор витрины> — <канонический URL или формулировка «см. таблицу точек доступа»>.

Если источник однозначно связан с карточкой ИС в wiki, добавьте одну строку:

- Карточка ИС: <[Название](/information-systems/<подраздел>/<slug>)> — <роль: официальная витрина / интерфейс к той же БД / смежный реестр>.

(удалите подпункт про ИС, если осмысленной связи нет)

## URL и точки доступа

| Тип доступа | URL | Формат |
| --- | --- | --- |
| <веб-раздел / API / FTP / каталог файлов> | <URL> | <HTML, JSON, CSV, …> |

## Объекты данных и показатели

Заполняй **конкретно** под источник (не копируйте один и тот же список во все карточки):

- <что является строкой набора: контракт, субсидия, строка исполнения по КБК и т.д.>
- <типичные ключи связки с другими наборами: ЕИНН, реестровый номер контракта, КБК, ОКТМО — если есть>
- <известные пробелы: нет справочника кодов, нет метаданных по единицам измерения и т.д.>

## Периодичность обновления

<Как часто обновляются данные или выгрузки: ежедневно, ежемесячно, по событию — по возможности.>

Если детали даны в карточке ИС, можно сослаться так: см. раздел «…» в <[Название ИС](/information-systems/<подраздел>/<slug>)> — **не** оставляйте голую отсылку «см. информационную систему» без URL и указания раздела.

(удалите раздел целиком, если ничего нельзя сказать содержательно)

## Ограничения и особенности

- <лицензия / условия использования, если известны>
- <объёмы, нестабильные URL, ограничения автоматизации, типичные ошибки 403/captcha>
- <качество: расхождения с официальной отчётностью, опечатки в кодах — если характерно>

## Примеры доступа

<Минимальный рабочий пример: запрос HTTP, фрагмент для скачивания FTP или команда CLI — без вымышленных endpoint’ов.>

(удалите раздел целиком, если рабочего примера нет — не оставляйте только закомментированный плейсхолдер)

```python
# пример только если реально проверен
```

## Связанные страницы wiki

- <[Другой источник данных](/data-sources/<подраздел>/<slug>)> — <чем дополняет или чем отличается>
- <[Информационная система](/information-systems/<подраздел>/<slug>)> — <связь>
- при необходимости: <[Отчётность](/reporting/<slug>)>, <[Термин глоссария](/glossary/<slug>)> — <зачем читателю перейти>

Дайте **минимум 2 содержательные ссылки**, если в wiki есть подходящие цели; не заменяйте перечень общей фразой «см. раздел источники данных».

## Не путать с

- **<Похожий источник или портал>** — <в чём отличие по данным и точкам доступа>

(удалите раздел целиком, если путаницы нет)

## История и контекст

<Для переноса нарратива со старых страниц: как менялся доступ, legacy-форматы. Для актуальных источников раздел обычно не нужен.>

(удалите раздел целиком, если нечего добавить к «Описание»)

## Сводка для машинной обработки

Опционально: краткие поля в YAML для копирования в промпты и скрипты. Удалите блок, если он дублирует frontmatter без пользы.

```yaml
primary_url: "https://..."
formats: []
update_cadence: "..."
join_keys: []
```

## Частые вопросы

**В:** <вопрос>  
**О:** <ответ по существу; не дублируйте пустыми отсылками к «Описание» и таблице в начале>

(удалите раздел целиком, если вопросы не добавляют специфики источнику)
````
