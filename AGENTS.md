# Инструкции для агента (репозиторий)

## Глоссарий (`wiki/glossary/`)

При **создании новой** или **обновлении существующей** карточки термина в каталоге `wiki/glossary/`:

1. Ориентируйся на шаблон **`wiki/glossary/glossary-card-template.md`**: таблица полей frontmatter + скелет внутри fenced-блока `markdown` внизу страницы. Переноси в новую карточку те же разделы и порядок, что в скелете.
2. Скопируй скелет из fenced-блока (не включая обёртку ` ```markdown `), замени плейсхолдеры; удаляй опциональные блоки («Не путать с», «Заметки для аналитики») только если они не применимы — тогда не оставляй пустых заголовков.
3. Не используй заглушки вроде «См. раздел [Глоссарий](/glossary/)» вместо **Связанные термины** — указывай конкретные ссылки вида `/glossary/<slug>` и одну строку пояснения к каждой связи.
4. Раздел **«Где встречается в учёте и данных»** не должен быть одинаковым во всех карточках: перечисляй только реально характерные для термина системы, отчёты и поля; при необходимости ссылайся на `wiki/data-sources/` и `wiki/information-systems/`.
5. **Частые вопросы**: ответы формулируй по существу; избегай пар «вопрос — см. определение», если ответ не добавляет ничего к тексту **Коротко** и **Определение**.
6. Файлы **не** считаются карточками термина: `README.md` (оглавление раздела), `glossary-card-template.md` (шаблон), `_category_.json` в подпапках (метаданные группы сайдбара).

Новую карточку клади в тематическую подпапку (`wiki/glossary/budget-system/`, `budget-classification/`, `procurement/`, `subsidies-and-transfers/`, `state-programs/`, `org-types/`, `other/`). В frontmatter добавляй `slug: /glossary/<slug>`, где `<slug>` совпадает с именем файла без `.md`, чтобы публичный URL оставался `/glossary/<slug>` и не ломались ссылки с других страниц.

## Информационные системы (`wiki/information-systems/`)

При **создании новой** или **обновлении существующей** карточки информационной системы в каталоге `wiki/information-systems/`:

1. Ориентируйся на шаблон **`wiki/information-systems/information-system-card-template.md`**: таблица полей frontmatter + скелет внутри fenced-блока `markdown` внизу страницы. Переноси в карточку те же разделы и порядок, что в скелете (при правке существующих страниц приведи структуру к шаблону по мере необходимости).
2. Скопируй скелет из fenced-блока (не включая обёртку ` ```markdown `), замени плейсхолдеры; удаляй опциональные блоки («Пользователи и типовые задачи», «Нормативная основа», «Ограничения и актуальность», «Связанные информационные системы») только если они не применимы — тогда не оставляй пустых заголовков.
3. Раздел **«Данные: объекты и связь с источниками»** не должен сводиться к общей отсылке на раздел источников данных: перечисляй типы объектов/показателей и давай **минимум 2–3 конкретные ссылки** на страницы `wiki/data-sources/` (с одной строкой пояснения к каждой), либо явно опиши отсутствие публичных наборов и укажи публичные производные (другие ИС, порталы).
4. В **«Связанные информационные системы»** используй ссылки вида `/information-systems/<подраздел>/<slug>` и пояснение связи (источник, зеркало, смежный реестр); не используй заглушки вместо конкретных целей перекрёстной ссылки.
5. **Частые вопросы**: ответы формулируй по существу; избегай пар «вопрос — см. описание выше», если ответ не добавляет ничего к **Назначение и границы** и таблице доступа.
6. Файлы **не** считаются карточками ИС: `README.md` в подкаталогах и корне раздела, `information-system-card-template.md` (шаблон).

Slug страницы совпадает с именем файла без `.md` и путём подраздела (пример: `/information-systems/federal/zakupki`).

## Источники данных (`wiki/data-sources/`)

При **создании новой** или **обновлении существующей** карточки источника данных в каталоге `wiki/data-sources/`:

1. Ориентируйся на шаблон **`wiki/data-sources/data-source-card-template.md`**: таблица полей frontmatter + скелет внутри fenced-блока `markdown` внизу страницы. Переноси в карточку те же разделы и порядок, что в скелете (при правке существующих страниц приведи структуру к шаблону по мере необходимости).
2. Скопируй скелет из fenced-блока (не включая обёртку ` ```markdown `), замени плейсхолдеры; удаляй опциональные блоки («Не путать с», «История и контекст», «Сводка для машинной обработки», «Частые вопросы») только если они не применимы — тогда не оставляй пустых заголовков.
3. В **«Связанные страницы wiki»** не используй заглушки вроде одной общей ссылки на каталог вместо перечня: указывай конкретные ссылки `/data-sources/<подраздел>/<slug>`, `/information-systems/<подраздел>/<slug>`, при необходимости `/reporting/<slug>` и `/glossary/<slug>` с одной строкой пояснения к каждой связи.
4. **«Периодичность обновления»** не должна сводиться к бессодержательной отсылке «см. ИС»: либо кратко опиши периодичность в карточке источника, либо дай **прямую** ссылку на карточку ИС и укажи раздел/смысл («периодичность публикации», «ограничения доступа» и т.п.), где это раскрыто.
5. **«Примеры доступа»**: не оставляй только закомментированный шаблон без рабочего примера — либо минимальный проверенный код или команда, либо удали раздел целиком.
6. Файлы **не** считаются карточками источника: `README.md` в подкаталогах и корне раздела, `data-source-card-template.md` (шаблон), `_category_.json` в подпапках федерального раздела (метаданные группы сайдбара).

Новую **федеральную** карточку клади в тематическую подпапку: `wiki/data-sources/federal/catalog/`, `budget/`, `zakupki/`, `programs-orgs/`, `statistics/`, `cbr/`, `nalog/` или `legacy/` (в корне `wiki/data-sources/federal/` остаётся только `README.md`). **Региональные**, **международные** и **гражданские** карточки — в `wiki/data-sources/regional/`, `wiki/data-sources/international/` или `wiki/data-sources/civil/` соответственно (без дополнительного уровня внутри этих папок, если не оговорено отдельно). В frontmatter указывай `slug: /data-sources/<подраздел>/<slug>`, где для федеральных источников публичный путь **без** имени подпапки: `slug: /data-sources/federal/<slug>`, а `<slug>` — имя файла без `.md`, чтобы URL не менялся при переносе между группами. Для `regional`, `international` и `civil`: `slug: /data-sources/regional/<slug>`, `slug: /data-sources/international/<slug>` и `slug: /data-sources/civil/<slug>` соответственно.

## Организации (`wiki/organizations/`)

При **создании новой** или **обновлении существующей** карточки организации в каталоге `wiki/organizations/`:

1. Ориентируйся на шаблон **`wiki/organizations/organizations-card-template.md`**: таблица полей frontmatter + скелет внутри fenced-блока `markdown` внизу страницы. Переноси в карточку те же разделы и порядок, что в скелете (при правке существующих страниц приведи структуру к шаблону по мере необходимости).
2. Скопируй скелет из fenced-блока (не включая обёртку ` ```markdown `), замени плейсхолдеры; удаляй опциональные блоки («Нормативная основа», «Не путать с», подраздел про источники в «Информационные системы и реестры», «Частые вопросы») только если они не применимы — тогда не оставляй пустых заголовков.
3. В **«Информационные системы и реестры»** не используй заглушку «См. раздел [Информационные системы](/information-systems/)» вместо перечня: указывай конкретные ссылки вида `/information-systems/<подраздел>/<slug>` и одну строку пояснения к каждой связи; при необходимости добавь конкретные ссылки на `wiki/data-sources/`.
4. В **«Связанные организации»** и **«Связанные термины глоссария»** давай конкретные ссылки `/organizations/<slug>` и `/glossary/<slug>` с пояснением строки; не заменяй их общими отсылками к разделам.
5. **Частые вопросы**: добавляй только если ответ даёт специфику организации; избегай повторения одних и тех же двух вопросов во всех карточках, если ответ не добавляет ничего к **Коротко**, **Роль в данных и учёте** и **Витрины и входы**.
6. Файлы **не** считаются карточками организации: `README.md` (оглавление раздела), `organizations-card-template.md` (шаблон).

Slug страницы совпадает с именем файла без `.md` (как в ссылках Docusaurus: `/organizations/minfin`).

## Отчётность и формы (`wiki/reporting/`)

При **создании новой** или **обновлении существующей** карточки отчётности в каталоге `wiki/reporting/`:

1. Ориентируйся на шаблон **`wiki/reporting/reporting-card-template.md`**: таблица полей frontmatter + скелет внутри fenced-блока `markdown` внизу страницы. Переноси в карточку те же разделы и порядок, что в скелете (при правке существующих страниц приведи структуру к шаблону по мере необходимости).
2. Скопируй скелет из fenced-блока (не включая обёртку ` ```markdown `), замени плейсхолдеры; удаляй опциональные блоки («Нормативная основа», «Периодичность и сроки публикации», «Связанные формы и отчётность», «Связанные информационные системы», «Не путать с», «Схема связей», «Особенности для аналитики», «Частые вопросы») только если они не применимы — тогда не оставляй пустых заголовков.
3. Раздел **«Где встречается в данных»** не должен сводиться к общей отсылке на раздел источников: перечисляй типы публикаций/наборов и давай **минимум 2–3 конкретные ссылки** на страницы `wiki/data-sources/` (с одной строкой пояснения к каждой), либо явно опиши отсутствие прямых открытых наборов и укажи публичные производные (портал, PDF, другая ИС) со ссылкой на соответствующую карточку.
4. В **«Связанные термины глоссария»** используй ссылки вида `/glossary/<slug>` и пояснение строки; не заменяй перечень общей отсылкой к глоссарию.
5. В **«Связанные формы и отчётность»** используй ссылки вида `/reporting/<slug>` и пояснение связи (дополняет, тот же горизонт на другом уровне, справка к форме и т.д.).
6. **Частые вопросы**: добавляй только если ответ даёт специфику формы; избегай пар «вопрос — см. описание выше», если ответ не добавляет ничего к **Коротко**, **Назначение и границы** и сводной таблице в начале.
7. Файлы **не** считаются карточками отчётности: `README.md` (оглавление раздела), `reporting-card-template.md` (шаблон).

Новую карточку клади в `wiki/reporting/`. В frontmatter добавляй `slug: /reporting/<slug>`, где `<slug>` совпадает с именем файла без `.md`, чтобы публичный URL оставался `/reporting/<slug>` и не ломались ссылки с других страниц. При необходимости добавь документ в `sidebars.ts` в группу `reporting`.

## Нормативные документы (`wiki/legal/`)

При **создании новой** или **обновлении существующей** карточки нормативного документа в каталоге `wiki/legal/`:

1. Ориентируйся на шаблон **`wiki/legal/legal-document-card-template.md`**: таблица полей frontmatter + скелет внутри fenced-блока `markdown` внизу страницы. Переноси в карточку те же разделы и порядок, что в скелете (при правке существующих страниц приведи структуру к шаблону по мере необходимости).
2. Скопируй скелет из fenced-блока (не включая обёртку ` ```markdown `), замени плейсхолдеры; удаляй опциональные блоки («История и контекст», «Сравнение и „не путать с“», «Наглядность», «Частые вопросы») только если они не применимы — тогда не оставляй пустых заголовков.
3. **Один акт — одна каноническая карточка**: не создавай параллельные файлы «кратко» и «полно» на один и тот же НПА; объединяй в один документ или оставь одну страницу с перекрёстной ссылкой с первых строк. При обновлении снимай дубли из `sidebars.ts` и из `wiki/legal/README.md`.
4. В **«Связанные страницы wiki»** не используй заглушки вроде одной общей ссылки на [Глоссарий](/glossary/) или [Информационные системы](/information-systems/) вместо перечня: указывай конкретные ссылки `/glossary/<slug>`, `/information-systems/<подраздел>/<slug>`, `/data-sources/<подраздел>/<slug>`, `/reporting/<slug>`, `/organizations/<slug>`, другие `/legal/<slug>` с одной строкой пояснения к каждой связи.
5. Раздел **«Где это в данных»** не должен быть одинаковым во всех карточках и не должен сводиться к общей отсылке на каталог источников: перечисляй только реально вытекающие из акта публикации, наборы и ИС; давай **минимум 2–3 конкретные ссылки** на страницы `wiki/data-sources/` и/или `wiki/information-systems/` (с пояснением строки), либо явно опиши отсутствие прямых открытых наборов и укажи публичные производные со ссылкой на соответствующую карточку wiki.
6. **Frontmatter**: заполняй `title`, `sidebar_label`, `tags` (первая метка `legal`, вторая — тема), `last_updated`; для новых карточек добавляй `slug: /legal/<slug>` (совпадает с именем файла без `.md`). Рекомендуй `description` (без лишнего экранирования скобок) и `doc_kind` из таблицы шаблона. Не смешивай смысл раздела с howto: не задавай `category: howto` и теги `howto` без необходимости — для нормативной базы приоритет меток `legal` и тематической второй метки.
7. **Частые вопросы**: добавляй только если ответ даёт специфику; избегай пар «вопрос — см. выше», если ответ не добавляет ничего к **Коротко** и **Назначение и границы применения**.
8. Файлы **не** считаются карточками нормативного документа: `README.md` (оглавление раздела), `legal-document-card-template.md` (шаблон).

Новую карточку клади в `wiki/legal/`. Добавь документ в `sidebars.ts` в группу «Нормативная база» и при необходимости строку в `wiki/legal/README.md`.

## Метаданные, машинный экспорт и PR (OpenSpec)

1. Для страниц под `wiki/` (кроме `*-template.md`) поддерживайте поля `description`, `last_updated`, `tags` из белого списка [`scripts/tag-allowlist.json`](scripts/tag-allowlist.json), `content_type` (`concept` \| `organization` \| `data_source` \| `howto` \| `legal` \| `reporting` \| `reference`) и при необходимости `entity_type` (`glossary` \| `organization` \| `information-system` \| `data-source` \| `reporting-form` \| `legal-document` \| `howto`).
2. Для карточек источников и фактологических страниц по возможности заполняйте `source_authority`, `rag_priority`, `related_pages`, `last_verified` — см. [`openspec/specs/corpus-metadata/spec.md`](openspec/specs/corpus-metadata/spec.md).
3. После крупных правок корпуса выполняйте `npm run export:knowledge` и коммитьте обновлённые [`exports/knowledge-index.jsonl`](exports/knowledge-index.jsonl) и [`exports/knowledge-graph.json`](exports/knowledge-graph.json).
4. Автоматические проверки в CI: `npm run lint:frontmatter`, `npm run lint:howto`, `npm run export:knowledge -- --check`, `npm run lint:qa`, `npm run lint:related-pages`, `npm run lint:corpus` — см. [`openspec/specs/ci-quality-gates/spec.md`](openspec/specs/ci-quality-gates/spec.md).
5. Стандартная структура опубликованных страниц в [`wiki/howto/`](wiki/howto/) — в [`openspec/specs/howto-playbook/spec.md`](openspec/specs/howto-playbook/spec.md); для массового выравнивания заголовков можно использовать служебный скрипт `scripts/inject-howto-playbook.mjs`.
